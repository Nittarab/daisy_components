<%# @label Button Variants %>
<%# @display height 200 %>

<div class="flex flex-wrap gap-2">
  <% variants.each do |variant| %>
    <%= render DaisyComponents::Actions::ButtonComponent.new(
      text: variant.titleize,
      variant: variant
    ) %>
  <% end %>
</div> 